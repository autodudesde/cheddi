{
  "name": "autodudes/cheddi-classic-compat",
  "description": "Libraries that newer TYPO3 cores ship and older ones do not. Loaded by DocumentExtractorService only when the class is missing, never registered up front.",
  "type": "project",
  "license": "GPL-2.0-or-later",
  "require": {
    "composer/pcre": "^3.1"
  },
  "config": {
    "vendor-dir": "ComposerVendor",
    "platform": {
      "php": "8.1.0"
    },
    "platform-check": false,
    "autoloader-suffix": "CheddiClassicCompat",
    "sort-packages": true,
    "allow-plugins": false
  }
}
